# IDE cache
.idea
# Secrets
.env
# Server build and background run files
/backend/bin/
/backend/server.pid
/backend/server.log
/backend/*.jsonl
# Benchmark profiles
/backend/bench-cpu.out
/backend/*.test
# Python bytecode
__pycache__/
# Training runs: TensorBoard logs and SB3 checkpoints. The export Go loads is policy.pb, which
# is not ignored - it is a build product of training that the server needs.
/rl-training/runs/
/rl-training/snapshots/
/rl-training/policy*.zip
